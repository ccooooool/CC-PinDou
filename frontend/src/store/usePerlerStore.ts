/**
 * 兼容层：原 God Store 已拆分为三个独立 Store
 * 新代码请直接导入对应子 Store，此文件将逐步废弃
 */

export { useEditorStore } from './useEditorStore';
export { useUIStore } from './useUIStore';
export { useConfigStore } from './useConfigStore';
